# Ce dossier contient des secrets (ex. clé de compte de service GCP / Vertex AI).
# Règle : le DOSSIER est suivi par git (via ce fichier), mais AUCUN de son contenu
# n'est jamais commité. On ignore tout, sauf ce .gitignore lui-même.
*
!.gitignore

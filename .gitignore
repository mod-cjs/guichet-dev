# Dépendances
node_modules/
node_modules
.pnp
.pnp.js

# Build
.next/
out/
dist/
tsconfig.tsbuildinfo

# Variables d'environnement - NE JAMAIS COMMITER
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.prod
.env.migration

# Prisma
prisma/migrations/*.sql.lock

# Logs
*.log
npm-debug.log*
logs/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Claude Code — settings locaux (contient tokens, ne pas commiter)
.claude/settings.local.json

data/migration_report_*.txt
data/migrate_all_report_*.json
data/create_sso_accounts_report_*.json
data/backfill_phones_report_*.json
data/backfill_sso_phones.sql
data/drupal_uid_cjs_uid_map.json
data/drupal_uid_cjs_uid_map_updated.json
data/create_sso_accounts.sql
data/migration-fixtures/

# Helper JIRA local (lit ~/.claude/.jira.env)
scripts/jira.sh

# Storybook build à la racine — ignoré (committé uniquement sous public/storybook/ pour servir via Vercel)
storybook-static/

# Sauvegardes locales de .env (secrets)
.env.local.bak-*

# Clés de compte de service GCP / Vertex AI (GOOGLE_APPLICATION_CREDENTIALS) — NE JAMAIS COMMITER
gcp-*.json
*-gcp-*.json
*service-account*.json
*service_account*.json
*-sa-key.json
vertex-*.json
# NB : le dossier secrets/ est suivi mais son contenu est ignoré par secrets/.gitignore

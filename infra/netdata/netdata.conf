# GUIC-545 — Config minimale : n'écouter QUE sur la boucle locale.
#
# `network_mode: host` (docker-compose.netdata.yml) est nécessaire pour que Netdata voie les
# VRAIES interfaces réseau de l'hôte (pas la vue isolée d'un conteneur) — mais ce mode désactive
# l'isolation de ports de Docker : sans cette directive, le tableau de bord (19999) serait exposé
# sur TOUTES les interfaces, y compris publiques. Même principe que Grafana
# (docker-compose.observabilite.yml) : accès par tunnel SSH uniquement, jamais 0.0.0.0.
#
# Le réseau et le disque sont deux visibilités SÉPARÉES (GUIC-710) : `network_mode: host` ne
# donne PAS la visibilité disque — c'est le bind mount `/:/host/root:ro,rslave`
# (docker-compose.netdata.yml) qui le fait, sans lien avec cette directive.

[global]
    # Serveur mutualisé (SSO + BRM + Guichet) : Netdata lui-même ne doit pas devenir la charge
    # qu'il surveille.
    memory mode = ram
    history = 3600

[web]
    bind to = 127.0.0.1:19999
    # GUIC-545 — challenge du 19/08 : la boucle locale + tunnel SSH obligatoire n'est qu'UNE
    # ligne de défense. Si elle saute un jour (mauvaise config future, port ouvert par erreur),
    # rien d'autre ne protège le tableau de bord — contrairement à Grafana, qui a toujours un
    # vrai mot de passe. Auth basique native, pas de proxy inverse nécessaire. Le fichier
    # .htpasswd est un secret : jamais committé, généré et monté depuis le serveur (voir
    # docs/netdata.md).
    require authentication = yes
    htpasswd file = /etc/netdata/.htpasswd

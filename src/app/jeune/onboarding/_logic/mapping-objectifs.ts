/** GUIC-689 — contrat posé, comportement non implémenté (voir le test associé). */
export function mapObjectif(_o: string): { domaines: string[]; types: string[] } {
  return { domaines: [], types: [] }
}

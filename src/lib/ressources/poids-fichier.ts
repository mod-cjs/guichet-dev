/**
 * GUIC-709 — Poids d'un fichier de ressource.
 *
 * NB (RED) — implémentation naïve volontaire, remplacée au commit GREEN.
 */
export async function poidsFichier(_url: string): Promise<number | null> {
  return null
}

export function formaterPoids(_octets: number | null): string | null {
  return null
}

export interface ObjectiveCardProps {
  objectif: string | null
  secteurs: string[]
  typesRecherches: string[]
  regionsMobilite: string[]
}

/** GUIC-689 — contrat posé, rendu non implémenté (voir le test associé). */
export function ObjectiveCard(_props: ObjectiveCardProps) {
  return null
}

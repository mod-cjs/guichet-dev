import type { EngagementItem } from '@/types/profil'

export interface SectionEngagementsProps {
  engagements: EngagementItem[]
}

/** GUIC-689 — contrat posé, rendu non implémenté (voir le test associé). */
export function SectionEngagements(_props: SectionEngagementsProps) {
  return null
}

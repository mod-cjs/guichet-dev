import type { ElementParcours } from '@/lib/profil-parcours'

export interface TimelineCardProps {
  parcours: ElementParcours[]
}

/** GUIC-689 — contrat posé, rendu non implémenté (voir le test associé). */
export function TimelineCard(_props: TimelineCardProps) {
  return null
}

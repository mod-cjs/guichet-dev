import type { LangueItem } from '@/types/profil'

export interface SkillsCardProps {
  competences: string[]
  langues: LangueItem[]
}

/** GUIC-689 — contrat posé, rendu non implémenté (voir le test associé). */
export function SkillsCard(_props: SkillsCardProps) {
  return null
}

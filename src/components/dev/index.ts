/**
 * Exports dev-only (refonte v2).
 *
 * USAGE STRICTEMENT DEV : ces composants rendent `null` en NODE_ENV
 * production. Réservés aux pages de preview/design canvas.
 */
export { PhoneFrame } from './PhoneFrame'
export type { PhoneFrameProps } from './PhoneFrame'
